--// Canonical list of maps approved for use in a round.
--// Names must match exactly the Model name inside ReplicatedStorage.Maps.
--// Add a new entry here whenever you add a new map to the Maps folder.

return {
	REGISTERED_MAPS = {
		-- "MapName1",
		-- "MapName2",
	},
}
